version: '3.8'

services:
  frontend:
    container_name: aria_frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: development
    ports:
      - "3004:5173"
    environment:
      - VITE_API_URL=http://localhost:5001
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - aria_network
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run dev -- --host

  backend:
    container_name: aria_backend
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: development
    ports:
      - "5001:5000"
    environment:
      - DATABASE_URL=mongodb://mongodb:27017/aria?replicaSet=rs0
      - REDIS_URL=redis://redis:6379
      - AGENT_URL=http://agent:8000
      - JWT_SECRET=${JWT_SECRET:-dev-secret-key-change-in-production-12345}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET:-dev-refresh-secret-key-change-in-production-67890}
      - NODE_ENV=development
      - PORT=5000
    volumes:
      - ./backend:/app
      - /app/node_modules
    depends_on:
      mongodb:
        condition: service_healthy
      agent:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - aria_network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:5000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    command: npm run dev

  agent:
    container_name: aria_agent
    build:
      context: ./agent
      dockerfile: Dockerfile
    ports:
      - "8002:8000"
    environment:
      - CLAUDE_API_KEY=${CLAUDE_API_KEY:-}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - BACKEND_URL=http://backend:5000
    volumes:
      - ./agent:/app
    networks:
      - aria_network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 20s
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

  redis:
    container_name: aria_redis
    image: redis:7-alpine
    ports:
      - "6381:6379"
    volumes:
      - redis_data:/data
    networks:
      - aria_network
    restart: unless-stopped

  mongodb:
    container_name: aria_mongodb
    image: mongo:7
    ports:
      - "27019:27017"
    environment:
      - MONGO_INITDB_DATABASE=aria
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]
    volumes:
      - mongodb_data:/data/db
    networks:
      - aria_network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 20s

volumes:
  redis_data:
  mongodb_data:

networks:
  aria_network:
    driver: bridge
